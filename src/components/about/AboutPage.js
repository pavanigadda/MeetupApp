import React from "react";

const AboutPage = () => (
  <div className="content-page">
    <p>
      This web application fetches the reactjs-dallas event information using
      React, Redux, React Router, and many more helpful libraries
    </p>
  </div>
);

export default AboutPage;
